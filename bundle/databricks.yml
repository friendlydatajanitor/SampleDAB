bundle:
  name: my_bundle_name

#include:
#  - resources/testbundle.yml

variables:
  email_address:
    description: "Email address for notifications"
  catalog:
    description: "Catalog name"
  cluster_id:
   description: "existing cluster"


targets:
  dev:
    workspace:
      host: https://dbc-e646edb0-d2dd.cloud.databricks.com/
      root_path: /Workspace/my-bundle
    mode: production
    default: true
    variables:
      #the values can be passed in via command line/CICD variables also
      email_address: mike.wingert@insight.com
      cluster_id: 0318-011637-fczmtwl0
      catalog: s3_catalog_test
